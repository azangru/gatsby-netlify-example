import React, { Component } from 'react';
import { Link, graphql } from "gatsby";

export default class BookPageTemplate extends Component {
  render() {
    // console.log('props', this.props);
    return <div>I am book page template!!</div>;
  }
}
